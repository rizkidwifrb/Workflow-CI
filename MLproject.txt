name: student-performance-ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "student_performance_processed.csv"}
    command: "python modelling.py --data {data_path}"